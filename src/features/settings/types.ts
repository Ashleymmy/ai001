export type ProviderPreset = {
  id: string
  name: string
  baseUrl?: string
  models: string[]
}

export type CustomProviderFormData = {
  name: string
  apiKey: string
  baseUrl: string
  model: string
  models: string[]
}

